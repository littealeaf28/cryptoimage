{
  "name": "CryptoImage",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prebuild": "npm install",
    "build": "concurrently \"cd backend && pip install -r requirements.txt\" \"cd frontend && npm install",
    "start": "concurrently \"cd backend && python server.py\" \"cd frontend && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
